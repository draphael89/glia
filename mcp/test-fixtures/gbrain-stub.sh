#!/usr/bin/env bash
# Deterministic gbrain stub for tests: ignores the query, returns two ranked
# fixture pages in the exact "[score] slug --" format retrieveContext parses.
echo "[0.91] note-alpha --"
echo "[0.80] note-beta --"
