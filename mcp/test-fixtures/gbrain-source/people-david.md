# people-david (FIXTURE self-page)
David is the test subject for the glia-context server. Values: legibility,
shipping, honest measurement. This self-page is intentionally longer than the
200-character validity threshold so the built-from-source fallback returns a
non-empty psyche with status "built" deterministically. FIXTURE-SELF-MARKER.
